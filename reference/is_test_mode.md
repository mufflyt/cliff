# TRUE when the current run is a TEST_MODE invocation. Reads the TEST_MODE environment variable (set by the test harness or CI).

TRUE when the current run is a TEST_MODE invocation. Reads the TEST_MODE
environment variable (set by the test harness or CI).

## Usage

``` r
is_test_mode()
```
