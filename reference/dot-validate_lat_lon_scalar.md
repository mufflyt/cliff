# Validate a single latitude/longitude pair

Validate a single latitude/longitude pair

## Usage

``` r
.validate_lat_lon_scalar(lat, lon)
```

## Arguments

- lat:

  \[numeric(1)\]: Latitude in decimal degrees.

- lon:

  \[numeric(1)\]: Longitude in decimal degrees.
