# Census-NPP single-year-of-age column names that sum to the women-65+ demand denominator. Returns c("POP_65", ..., "POP_100"). Apply npp_total_female() first to select the female rows.

Census-NPP single-year-of-age column names that sum to the women-65+
demand denominator. Returns c("POP_65", ..., "POP_100"). Apply
npp_total_female() first to select the female rows.

## Usage

``` r
npp_women_65plus_cols()
```
