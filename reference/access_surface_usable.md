# Is an access surface usable (non-NULL with at least one tract)?

Is an access surface usable (non-NULL with at least one tract)?

## Usage

``` r
access_surface_usable(surface)
```

## Arguments

- surface:

  a \[read_access_surface()\] result (or \`NULL\`).

## Value

logical scalar.
