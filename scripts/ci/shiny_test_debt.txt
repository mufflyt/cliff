# Shiny application tests that currently fail.
#
# CI fails when this set GROWS. Shrink it by fixing the app or the expectation
# and deleting the line; never add a line to make CI pass.
#   Rscript scripts/ci/run_shiny_suites.R --write-debt
#
# Baselined on ubuntu-latest. These are shinytest2 BROWSER tests: they skip
# locally unless NOT_CRAN=true and Chrome is present, and they run on the
# runner, so a macOS-baselined registry will not match CI.
#
# STATUS: was 5 of 5 driver tests failing; now 3. The SSOT rewiring fixed the
# two that failed because the app refused to launch (it validated a 1,306 model
# against 1,339 expectations).
#
# The residue is NOT an SSOT problem and should not be treated as one. These
# assert rendered UI strings that have drifted from the app's markup -- e.g.
# "projected active headcount in 2029" does not appear anywhere in app.R any
# more -- and #exec_cards comes back empty under AppDriver, which points at the
# harness or a render-timing issue rather than at any number. Fixing them is a
# UI-test refresh, separate from the SSOT work.
shiny_urps_scenarios :: test-app-driver.R :: Best estimate shows the frozen headline numbers
shiny_urps_scenarios :: test-app-driver.R :: app boots past the validation gate and shows the baseline
shiny_urps_scenarios :: test-app-driver.R :: the age table rounds counts and flags the sparse 70+ estimate
