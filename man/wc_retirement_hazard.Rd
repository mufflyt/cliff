% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wc_retirement_hazard.R
\name{wc_retirement_hazard}
\alias{wc_retirement_hazard}
\title{Resolve the retirement hazard the projection will run on.}
\usage{
wc_retirement_hazard(
  source = WC_RETIREMENT_SOURCES,
  band_labels,
  band_of,
  legacy_band_ev = NULL,
  legacy_band_py = NULL,
  confirmation_window_months = 12L,
  ns = "mufflyaccess"
)
}
\arguments{
\item{source}{One of `WC_RETIREMENT_SOURCES`. Default `"legacy_modeled"`.}

\item{band_labels}{The engine's age-band labels (`eng$WC_BAND_LABELS`) -- the
order the returned `band_ev` / `band_py` / `hz_point` vectors follow.}

\item{band_of}{A function age -> band label (`eng$wc_band_of`), reused so the
observed hazard is banded EXACTLY as the engine bands ages (no reimplementation).}

\item{legacy_band_ev, legacy_band_py}{The frozen band event / person-year counts,
required for `"legacy_modeled"` (passed straight through, so the legacy path
is byte-identical to the caller's own constants).}

\item{confirmation_window_months}{Recorded in provenance; should mirror the
mufflyaccess manifest `exit_confirmation_months` (default 12).}

\item{ns}{The mufflyaccess namespace (overridable in tests with a stub env).}
}
\value{
A list: `retirement_source`, `band_labels`, `band_ev`, `band_py`,
  `hz_point` (named by `band_labels`; `NA` for a band with no person-years, which
  the engine fills with the max hazard), and `provenance` (a named list carrying
  `retirement_source`, `hazard_artifact`, `hazard_version`, `hazard_hash`,
  `ascertainment_status`, `confirmation_window_months`, `uncertainty_method`).
}
\description{
Resolve the retirement hazard the projection will run on.
}
