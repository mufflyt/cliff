% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wait_adequacy.R
\name{erlang_c}
\alias{erlang_c}
\title{Erlang-C delay probability (probability an arrival must wait)}
\usage{
erlang_c(s, a)
}
\arguments{
\item{s}{Number of parallel servers: a single positive integer.}

\item{a}{Offered load in Erlangs (`lambda / mu`): a single non-negative scalar,
strictly less than `s` (stability).}
}
\value{
A single probability in `[0, 1]`.
}
\description{
The Erlang-C formula C(s, a): in a stationary M/M/s queue with `s` servers and
offered load `a = lambda / mu` Erlangs, the probability that an arriving
customer finds all servers busy and joins the queue. Derived from Erlang-B via
`C = s B / (s - a (1 - B))`. Requires a stable queue, `a < s`
(equivalently utilisation `rho = a / s < 1`); an unstable load fails loudly.
}
\examples{
erlang_c(3, 2)      # P(wait) with 3 servers, 2 Erlangs offered
}
\seealso{
[erlang_b()]; [mmc_wait_in_queue()] for the mean wait this feeds.

Other wait-adequacy: 
\code{\link{erlang_b}()},
\code{\link{mmc_wait_in_queue}()},
\code{\link{wait_to_adequacy}()}
}
\concept{wait-adequacy}
