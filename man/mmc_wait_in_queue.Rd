% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wait_adequacy.R
\name{mmc_wait_in_queue}
\alias{mmc_wait_in_queue}
\title{Mean wait in queue for a stationary M/M/s system}
\usage{
mmc_wait_in_queue(s, mu, rho)
}
\arguments{
\item{s}{Number of parallel servers: a single positive integer.}

\item{mu}{Per-server service rate: a single positive finite scalar (completed
visits per unit time per server).}

\item{rho}{Server utilisation `lambda / (s mu)`: a single scalar in `(0, 1)`.}
}
\value{
A single non-negative mean wait, in `1 / mu` time units. Strictly
  increasing in `rho`; `-> 0` as `rho -> 0` and `-> Inf` as `rho -> 1-`.
}
\description{
Expected time an arrival spends waiting *before* service (W_q, excluding the
service time itself) in an M/M/s queue with `s` servers, per-server service
rate `mu`, and utilisation `rho`. Uses `W_q = C(s, a) / (s mu (1 - rho))` with
`a = rho s`. Returned in the reciprocal time unit of `mu` (if `mu` is
patients per month, `W_q` is in months).
}
\examples{
mmc_wait_in_queue(s = 4, mu = 2, rho = 0.8)
}
\seealso{
[wait_to_adequacy()], the inverse of this map.

Other wait-adequacy: 
\code{\link{erlang_b}()},
\code{\link{erlang_c}()},
\code{\link{wait_to_adequacy}()}
}
\concept{wait-adequacy}
