% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/urps_procedure_codes.R
\name{urps_anchor_field}
\alias{urps_anchor_field}
\title{Specialty field (OBG / URO) for URPS anchor procedure codes}
\usage{
urps_anchor_field(codes)
}
\arguments{
\item{codes}{Character vector of HCPCS codes (typically a subset of
[urps_anchor_codes()]).}
}
\value{
A character vector the same length as `codes`: `"OBG"` for the
  surgical / reconstructive anchors, `"URO"` for the functional / urodynamic
  anchors, and `NA` for any code not present in `URPS_ANCHOR_PROCEDURES`.
}
\description{
Look up the canonical OB-GYN-field vs urology-field classification for a
vector of anchor procedure codes, so consumers never re-hardcode the
`c("OBG", "OBG", "URO", "URO")` field vector.
}
\examples{
urps_anchor_field(c("57288", "51728"))   # c("OBG", "URO")
}
\seealso{
[urps_anchor_codes()]; the `URPS_ANCHOR_PROCEDURES` lookup table.
}
