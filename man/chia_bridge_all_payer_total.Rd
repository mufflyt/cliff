% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chia_medicare_bridge.R
\name{chia_bridge_all_payer_total}
\alias{chia_bridge_all_payer_total}
\title{Aggregate an all-payer bridge result over its identified bands}
\usage{
chia_bridge_all_payer_total(bridged)
}
\arguments{
\item{bridged}{The `data.frame` returned by [bridge_medicare_to_all_payer()].}
}
\value{
A list with `total` (sum of `all_payer_volume` over identified bands;
  `NA_real_` if none), `fully_identified` (logical: all input bands
  identified), `n_identified`, `n_total`, and `unidentified_bands` (the
  `age_lower` values that were refused).
}
\description{
Sum the identified all-payer volume and report whether every requested band
resolved. The `fully_identified` flag is the hook the capacity-evidence gate
keys on: an all-payer workload total is only admissible when no band was
silently dropped.
}
\examples{
md <- data.frame(age_lower = c(65L, 75L), medicare_volume = c(400, 300))
chia_bridge_all_payer_total(bridge_medicare_to_all_payer(md))
}
\seealso{
[bridge_medicare_to_all_payer()]

Other chia-medicare-bridge: 
\code{\link{bridge_medicare_to_all_payer}()},
\code{\link{chia_bridge_contract}()},
\code{\link{validate_chia_bridge_contract}()}
}
\concept{chia-medicare-bridge}
